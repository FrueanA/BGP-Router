# BGP-Router
A simplified BGP router. Supports route updates/withdrawals, longest-prefix matching, forwarding logic, JSON-encoded messages, aggregation, and full interaction with a provided simulator. Passes all project test configurations.
